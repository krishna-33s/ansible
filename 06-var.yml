- name: playbook variable
  hosts: local
  connection: local
  vars:  
    data.yml

  tasks:
  - name: taskbook variable
    vars:
      NAME: "Ravi"
    ansible.builtin.debug:
      msg: "My Name Is: {{NAME}}, Age Is: {{AGE}}"