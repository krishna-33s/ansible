- name: shell
  hosts: local
  connection: local

  tasks:
  - name: command
    ansible.builtin.command: ls -l
    register: printing

  - name: print output
    ansible.builtin.debug:
      msg: "{{ user_passwd }}"

  # - name: command
  #   ansible.builtin.shell:cat /etc/passwd | grep root
  #   register: user_root

  # - name: print root user_passwd
  #   ansible.builtin.debug:
  #     msg: "{{ user_root }}"