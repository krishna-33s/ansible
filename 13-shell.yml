- name: shell
  hosts: local
  connection: local

  tasks:
  - name: shell
    ansible.builtin.command: echo "krishna"
    register: user_passwd

  - name: print output
    ansible.builtin.debug:
      msg: "{{ user_passwd }}"