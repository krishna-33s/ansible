- name: install nginx
  hosts: web
  become: true

  tasks:
  - name: nginx installation
    ansible.builtin.package:
      name: nginx
      state: present

  - name: start nginx
    ansible.builtin.service:
      name: nginx    
      state: start
      enabled: yes